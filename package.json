{
  "name": "snailbird-content",
  "version": "0.0.1",
  "description": "Content server for media for snailbird",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "run": "node ./build/src",
    "debug": "node --inspect ./build/src",
    "go": "npm run build && npm run run",
    "bd": "npm run build && npm run debug",
    "test": "npm run build && node --test ./build/test/index.test.js",
    "testi": "npm run build && node --test --inspect ./build/test/index.test.js"
  },
  "files": [
    "./build/**/*"
  ],
  "author": "Daniel Harvey",
  "type": "module",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
