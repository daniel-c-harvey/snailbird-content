{
  "name": "snailbird-content",
  "version": "0.0.1",
  "description": "Content server for media for snailbird",
  "main": "./build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "run": "node ./build",
    "go": "tsc && node ./build",
    "debug": "tsc && node --inspect --trace-promises ./build"
  },
  "files": [
    "./build/"
  ],
  "author": "Daniel Harvey",
  "type": "module",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
